services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "8765:8765"
    env_file:
      - .env
    environment:
      - SERVER_URL=https://revise.mrinal.dev
